# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/uniguide"

# Secret used to sign NextAuth JWTs. Generate with:
# openssl rand -base64 32
AUTH_SECRET=""

# Set to true when running on a non-Vercel host (needed for NextAuth)
AUTH_TRUST_HOST=true
